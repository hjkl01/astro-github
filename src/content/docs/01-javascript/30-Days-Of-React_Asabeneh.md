
---
title: 30-Days-Of-React
---


# 30-Days-Of-React（Asabeneh）

**项目地址**  
<https://github.com/Asabeneh/30-Days-Of-React>

## 项目简介
- 30 天内完成 30 个 React 小项目，覆盖从基础到进阶的核心概念。  
- 每天都包含完整的项目结构、代码实现与可运行示例，帮助快速上手并巩固知识。  
- 适合初学者、想复习 React 基础与进阶技巧的开发者。

## 主要特性
| 天数 | 项目名称 | 核心技术点 |
|------|----------|-------------|
| 1 | 计数器 | React 基础、JSX、事件处理 |
| 2 | 计数器（使用 Hook） | `useState`、函数式组件 |
| 3 | Todo List | 状态提升、数组操作 |
| 4 | 计时器 | `useEffect`、定时器 |
| 5 | 条件渲染 | `if`、`ternary`、`&&` |
| 6 | 表单 | `onChange`、受控组件 |
| 7 | 计算器 | 事件委托、数学运算 |
| 8 | 计数器（自定义 Hook） | 自定义 Hook、复用逻辑 |
| 9 | 电影搜索 | `fetch API`、RESTful 调用 |
| 10 | 电影卡片 | CSS Grid、Flexbox |
| 11 | 计数器（使用 Context） | `React Context`、全局状态 |
| 12 | 计数器（Redux） | Redux、Provider、Reducer |
| 13 | 计数器（React Query） | `react-query`、数据请求 |
| 14 | Todo List（React Query） | `react-query`、缓存 |
| 15 | 计数器（React Suspense） | `Suspense`、延迟渲染 |
| 16 | 计数器（React Router） | `react-router-dom`、路由 |
| 17 | 计数器（React Router + Hook） | 路由参数、Hook |
| 18 | 计数器（React Router + State） | 状态传递、导航 |
| 19 | 计数器（React Router + Event） | 事件传递、历史 |
| 20 | 计数器（React Router + API） | API 调用、路由 |
| 21 | 计数器（React Router + Auth） | 认证、权限 |
| 22 | 计数器（React Router + Local Storage） | 本地存储、持久化 |
| 23 | 计数器（React Router + Redux） | Redux + 路由 |
| 24 | 计数器（React Router + React Query） | React Query + 路由 |
| 25 | 计数器（React Router + Firebase） | Firebase 认证、数据库 |
| 26 | 计数器（React Router + Apollo） | GraphQL、Apollo Client |
| 27 | 计数器（React Router + Stripe） | Stripe 付款、API |
| 28 | 计数器（React Router + Socket.io） | 实时通信、Socket.io |
| 29 | 计数器（React Router + GraphQL） | GraphQL、Apollo Server |
| 30 | 计数器（React Router + Auth0） | Auth0 认证、授权 |

> **说明**  
> - 每个项目均自包含完整文件结构。  
> - 代码遵循 ES6+ 语法，使用 Create React App 或 Vite（部分项目）作为构建工具。  
> - 通过 `npm start` 或 `yarn start` 可以立即运行对应目录中的项目。

## 用法

1. **克隆仓库**  
   ```bash
   git clone https://github.com/Asabeneh/30-Days-Of-React.git
   cd 30-Days-Of-React
   ```

2. **安装依赖**  
   ```bash
   npm install          # 或 yarn install
   ```

3. **运行指定天数的项目**  
   - 进入对应目录，例如 Day-01：  
     ```bash
     cd Day-01
     npm start
     ```
   - 浏览器打开 `http://localhost:3000` 即可看到运行效果。

4. **探索更多**  
   - 每个 Day 文件夹里都有 `README.md`，内含项目说明、技术要点和实现细节。  
   - 你可以直接修改代码，尝试添加功能，或把项目迁移到 Vite、Next.js 等框架。

## 贡献

- Fork 本仓库 → 创建 Feature 分支 → Push 代码 → Pull Request。  
- 欢迎添加新的项目、改进现有实现或修复 bug。

祝你学习愉快 🚀

--- 
**文件路径**  
`src/content/docs/00/30-Days-Of-React_Asabeneh.md`
